import { createFileRoute } from "@tanstack/react-router";
import { AppShell } from "@/components/shell/AppShell";
import { useQuery } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/leaderboard")({component:LeaderboardPage});
function LeaderboardPage(){const{data=[],isLoading}=useQuery({queryKey:["leaderboard"],queryFn:async()=>{const{data,error}=await supabase.from("profiles").select("display_name,xp,level,streak,avatar_config").order("xp",{ascending:false}).limit(50);if(error)throw error;return data??[]}});return <AppShell title="Leaderboard" subtitle="Learn, progress, and climb"><div className="space-y-2">{isLoading&&<p className="text-sm text-muted-foreground">Loading leaderboard…</p>}{!isLoading&&!data.length&&<p className="surface-glass rounded-2xl p-5 text-sm text-muted-foreground">No learners yet.</p>}{data.map((p,i)=><div key={`${p.display_name}-${i}`} className="surface-glass flex items-center gap-3 rounded-2xl p-3"><span className="w-7 text-center font-display font-bold">#{i+1}</span><div className="min-w-0 flex-1"><p className="truncate font-semibold">{p.display_name}</p><p className="text-xs text-muted-foreground">Level {p.level} · {p.streak} day streak</p></div><span className="text-sm font-bold text-primary">{p.xp.toLocaleString()} XP</span></div>)}</div></AppShell>}
