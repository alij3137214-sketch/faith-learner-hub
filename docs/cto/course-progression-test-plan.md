# Course progression verification

Target flow: authenticated learner → learning path → lesson → completion → persisted progress/reward → quiz → submission.

The browser test must exercise the real production UI and authenticated Supabase RPCs. It must not inspect quiz answer keys or manufacture XP.
